#include <bits/stdc++.h>
using namespace std;

int main() {
	cin.tie(0)->sync_with_stdio(0);
	int n, ans = 0; cin >> n;
	string y, t; cin >> y >> t;
	for (int i = 0; i < n; i++) {
		if (y[i] == 'C' && t[i] == 'C') ans++;
	}
	cout << ans;
}