#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(0)->sync_with_stdio(0);
    int n, a, b, s, t; cin >> n >> a >> b >> s >> t;
    if (s > t) swap(s, t);
    if (t <= a || b <= s) cout << "Outside";
    else if (a < s && t < b) cout << "City";
    else cout << "Full";
}