#include <bits/stdc++.h>
using namespace std;

int main() {
	int n; cin >> n;
	bool tri = false;
	for (int i = 0; i < n; i++) {
		string s; cin >> s;
		if (s == "anj") {
			cout << "뭐야;";
			return 0;
		}
	}
	cout << "뭐야?";
}