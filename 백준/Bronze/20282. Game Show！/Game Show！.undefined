#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
	cin.tie(0)->sync_with_stdio(0);
	int n, ans = 100, cur = 100; cin >> n;
	for (int i = 0; i < n; i++) {
		int p; cin >> p;
		cur += p;
		ans = max(ans, cur);
	}
	cout << ans;
}