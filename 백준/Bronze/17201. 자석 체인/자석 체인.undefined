#include <bits/stdc++.h>
using namespace std;

int main() {
	cin.tie(0)->sync_with_stdio(0);
	int n; cin >> n;
	string s; cin >> s;
	for (int i = 1; i < n; i++) {
		if (s[i * 2] != s[i * 2 - 2] || s[i * 2 + 1] != s[i * 2 - 1]) { cout << "No"; return 0; }
	}
	cout << "Yes";
}