#include <bits/stdc++.h>
using namespace std;

int main() {
	cin.tie(0)->sync_with_stdio(0);
	int t; cin >> t; while (t--) {
		int n, mx = -INT_MAX, mn = INT_MAX; cin >> n;
		for (int i = 0; i < n; i++) {
			int p; cin >> p;
			mx = max(mx, p);
			mn = min(mn, p);
		}
		cout << mn << " " << mx << '\n';
	}
}