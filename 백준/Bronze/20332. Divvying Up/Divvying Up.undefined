#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
	cin.tie(0)->sync_with_stdio(0);
	int sum = 0, n; cin >> n;
	for (int i = 0; i < n; i++) {
		int p; cin >> p;
		sum += p;
	}
	cout << (sum % 3 == 0 ? "yes" : "no");
}