#include <bits/stdc++.h>
using namespace std;

int num[5];

int main() {
    cin.tie(0)->sync_with_stdio(0);
    for (int i = 0; i < 5; i++) cin >> num[i];

    int mn = INT_MAX;
    for (int i = 0; i < 5; i++) {
        for (int j = i + 1; j < 5; j++) {
            for (int k = j + 1; k < 5; k++) {
                int g = lcm(lcm(num[i], num[j]), num[k]);
                mn = min(mn, g);
            }
        }
    }
    cout << mn;
}