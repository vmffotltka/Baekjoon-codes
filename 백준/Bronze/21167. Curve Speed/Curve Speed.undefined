#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(0)->sync_with_stdio(0);
    double r, s;
    while (cin >> r >> s) {
        cout << round(sqrt((r * (s + 0.16)) / 0.067)) << '\n';
    }
}