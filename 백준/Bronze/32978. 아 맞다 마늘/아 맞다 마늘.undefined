#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(0)->sync_with_stdio(0);
    int n; cin >> n;
    vector<string> ingredients(n);
    for (int i = 0; i < n; i++) {
        cin >> ingredients[i];
    }
    for (int i = 0; i < n - 1; i++) {
        string s; cin >> s;
        remove(ingredients.begin(), ingredients.end(), s);
    }
    cout << ingredients[0];
}