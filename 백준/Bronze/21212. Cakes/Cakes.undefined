#include <bits/stdc++.h>
using namespace std;

int main() {
	cin.tie(0)->sync_with_stdio(0);
	int n, ans = INT_MAX; cin >> n;
	for (int i = 0; i < n; i++) {
		int a, b; cin >> a >> b;
		ans = min(ans, b / a);
	}
	cout << ans;
}