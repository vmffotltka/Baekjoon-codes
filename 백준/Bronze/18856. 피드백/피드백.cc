#include <bits/stdc++.h>
using namespace std;

int main() {
	cin.tie(0)->sync_with_stdio(0);
	int n; cin >> n;
	cout << n << '\n';
	for (int i = 1; i <= n - 1; i++) {
		cout << i << " ";
	}
	cout << 53;
}