#include <bits/stdc++.h>
using namespace std;

int main() {
	cin.tie(0)->sync_with_stdio(0);
	int n, ans = 0; cin >> n;
	for (int i = 0; i < n; i++) {
		int p; cin >> p;
		ans += min(7, p) - (i & 1 ? 3 : 2);
	}
	cout << ans;
}