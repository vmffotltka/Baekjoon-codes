#include <bits/stdc++.h>
using namespace std;

int dp[46][2];

int main() {
	cin.tie(0)->sync_with_stdio(0);
	int n; cin >> n;
	dp[0][0] = 1;
	for (int i = 1; i <= n; i++) {
		dp[i][0] = dp[i - 1][1];
		dp[i][1] = dp[i - 1][0] + dp[i - 1][1];
	}
	cout << dp[n][0] << ' ' << dp[n][1];
}